# Vigil 1.0.0 (25) release record

**Release status: candidate; not yet uploaded. Build 24 remains submitted for App Review.**

> Last reviewed: 2026-08-12
>
> Review again: after each build, upload, TestFlight, or App Review state change

This is the canonical evidence record for version `1.0.0`, build `25`. Build
`24` remains documented in [its release record](1.0.0-24.md). Build 25 is the
replacement candidate for the Grok Build provider-contract regression.

## Identity

| Field | Recorded value |
|---|---|
| App Store Connect Apple ID | `6792373775` |
| Version | `1.0.0` |
| Build | `25` |
| Artifact commit | Assigned after the candidate is merged to `origin/main` |
| ASC build ID | Not assigned before upload processing |
| Version ID | `3b219c79-dcc3-4cec-a4dd-9f8e575c65f3` |
| Current build-24 review submission ID | `57c3cd77-3e1e-430c-8031-d765c441ef7b` |
| Replacement review submission ID | Not assigned before resubmission |
| Archive path | Assigned after the exact merged artifact commit passes CI |
| Intended release | Public App Store after App Review approval |
| Release runbook | [`docs/development/release.md`](../development/release.md) |

## Release scope

Build 25 changes only the Grok Build usage contract and its supporting tests,
fixtures, and documentation:

- Requests the same `format=credits` billing representation used by the
  current Grok CLI.
- Maps the provider's shared weekly `creditUsagePercent` and
  `currentPeriod.end` reset without inventing exact used or limit values.
- Retains compatibility for older responses that contain a positive monthly
  used/limit pair.
- Rejects the retired bare-endpoint response where a zero monthly limit is
  returned alongside nonzero usage, which caused build 24 to show **Provider
  changed**.

No intentional Settings, account-storage, privacy, widget, or notification
behavior changes relative to build 24.

## Gate status

| Gate | State | Evidence |
|---|---|---|
| Live root-cause reproduction | Passed | Authenticated local comparison on 2026-08-12: bare billing returned the retired zero-limit monthly shape; `format=credits` returned the current weekly shape |
| Current App Store state | Passed | Build 24 and submission `57c3cd77-…` were `WAITING_FOR_REVIEW`; next available build number was 25 on 2026-08-12 |
| Version/build source | Passed | `MARKETING_VERSION=1.0.0`, `CURRENT_PROJECT_VERSION=25` in `apps/apple/project.yml` |
| Complete VigilKit suite | Passed locally | 207 executed, 2 live-auth skipped, 0 failures on 2026-08-12; rerun from the exact merged release worktree before archive |
| Documentation, iOS tests, CI, signing, archive, upload, TestFlight, and App Review replacement | Pending | Recorded here as each release gate completes |

## Delivered-build acceptance

After Internal TestFlight receives build 25, verify a linked Grok Build account
shows the weekly percentage and reset instead of **Provider changed**. Repeat
the build 24 smoke checks for account refresh, history, widget privacy,
automatic-check pause, and background/foreground transitions before the App
Review replacement is treated as complete.
