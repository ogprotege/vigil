# Vigil 0.15.0 (20) release record

**Release status: artifact candidate prepared. Local gates, CI on the artifact commit, archive, verification, and the explicit upload approval remain pending.**

> Last reviewed: 2026-07-29
>
> Review again: after every completed, failed, or invalidated release gate

This is the canonical evidence record for version `0.15.0`, build `20`.
Build `19` remains documented in [its completed upload record](0.15.0-19.md).

## Identity

| Field | Recorded value |
|---|---|
| App Store Connect Apple ID | `6792373775` |
| Version | `0.15.0` |
| Build | `20` |
| Candidate branch | `release/0.15.0-20-artifact` |
| Artifact candidate | Recorded after the artifact pull request merges |
| Integration commit | Recorded after the artifact pull request merges |
| Internal TestFlight group | `Internal` |
| Release runbook | [`docs/development/release.md`](../development/release.md) |

## Release scope

Build 20 removes every App Group file lock from the app's launch path. Build
19 was killed on a TestFlight device with RUNNINGBOARD `0xdead10cc` 1.3
seconds into a never-foregrounded launch: `AppModel` construction ran inside
`App.main()` before UIApplicationMain and held the per-account snapshot flock
while JSON-decoding when the system suspended the process. No background-task
assertion can exist that early, so the guard approach from build 18 could not
cover this path.

The launch initializer now performs no App Group locking. The initial disk
load moved to the idempotent `AppModel.ensureLoadedFromDisk()`, which runs
under `SuspensionGuard` from scene activation, the root scene task, and the
`BGAppRefreshTask` handler. Presentation shows a launch frame until the load
completes. Identity mutations and refresh-all load persisted state before
acting so an index write from an unloaded model cannot erase accounts already
on disk. `SnapshotStore` holds its per-account flock only for the raw byte
copy and rotation — encoding before the lock, read-path decoding after
release — in both the app and widget processes.

The fix landed through [PR #38](https://github.com/ogprotege/vigil/pull/38),
merged as `34f3bc3`.

## Gate status

| Gate | State | Evidence |
|---|---|---|
| Fix pull request | Merged | [PR #38](https://github.com/ogprotege/vigil/pull/38) merged as `34f3bc3`; push and PR `vigilkit` runs passed on fix head `1d3fcd5` |
| Artifact pull request | Pending | This record ships with the artifact candidate |
| Exact candidate identity and clean-worktree preflight | Pending | Runs against the merged artifact commit |
| Build settings from Xcode | Pending | Expect `MARKETING_VERSION=0.15.0`, `CURRENT_PROJECT_VERSION=20`, `VALIDATE_PRODUCT=YES` |
| Documentation gate on artifact candidate | Pending | `scripts/check-docs.sh` |
| VigilKit suite on artifact candidate | Pending | `swift test --package-path packages/VigilKit` |
| Full iOS scheme on artifact candidate | Pending | Simulator run recorded with result bundle |
| Property-list, privacy-manifest, and entitlement lint | Pending | `plutil -lint` across all six files |
| Merged-main CI | Pending | Requires green checks on the artifact commit |
| Apple service preflight | Pending | Checked before spending the build number and again before upload |
| Distribution signing preflight | Pending | Installed identity and both AppStore profiles |
| Fresh signed archive | Pending | Created only from the exact artifact commit |
| Archive verifier | Pending | `scripts/verify-ios-archive.sh` |
| App Store Connect collision preflight | Pending | Zero existing records for version `0.15.0`, build `20` |
| Immediate pre-upload refresh | Pending | Status, group, export options, second verifier pass |
| Exact upload approval | Pending | Requires the runbook's explicit approval sentence after the verified archive exists |
| App Store Connect upload | Pending | Blocked on the approval gate |
| Processing and Internal distribution | Pending | Blocked on upload |
| Physical-device acceptance | Pending | Runbook step 11 remains with the repo owner |

## Remaining work

Upload and distribution require the explicit approval gate in
[`docs/development/release.md`](../development/release.md) after the verified
archive exists. The build 18/19 physical-device acceptance walk remains
outstanding and carries over to this build.
