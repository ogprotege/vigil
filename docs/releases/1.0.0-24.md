# Vigil 1.0.0 (24) release record

**Release status: source candidate for the 0xdead10cc lifecycle-lock fix. Local gates must pass before archive, upload, or App Store resubmission.**

> Last reviewed: 2026-08-10
>
> Review again: after every completed, failed, or invalidated release gate

This is the canonical evidence record for version `1.0.0`, build `24`. Build
`23` remains documented in [its release record](1.0.0-23.md).

## Identity

| Field | Recorded value |
|---|---|
| App Store Connect Apple ID | `6792373775` |
| Version | `1.0.0` |
| Build | `24` |
| Artifact candidate | Pending green local suite + merged commit; no signed archive yet |
| Intended release | Replace 1.0.0 (23) as the public App Store / TestFlight candidate after green gates |
| Release runbook | [`docs/development/release.md`](../development/release.md) |

## Release scope

Build 24 is a reliability fix over the build 23 public candidate:

- Closes the TestFlight `RUNNINGBOARD 0xdead10cc` kill observed on 1.0.0 (23): overnight history reload held the account-lifecycle App Group flock (`captureActiveGeneration` → `FileManager.fileExists`) while iOS suspended the process.
- Structural choke point: every `AccountLifecycleStore` exclusive lock begins under `SuspensionGuard` before `flock`.
- Guard-ordering fixes for history append/backfill, account retirement deletes, pending-event loads, and post-link snapshot reads.
- Widget omits `VIGIL_APP_HOST` / `SuspensionGuard` (accepted residual).

No intentional product or Settings behavior change relative to build 23.

## Gate status

| Gate | State | Evidence |
|---|---|---|
| Version/build source | Passed | `MARKETING_VERSION=1.0.0`, `CURRENT_PROJECT_VERSION=24` in `apps/apple/project.yml` |
| Complete VigilKit suite | Passed | 206 tests executed, 2 live-auth skipped, 0 failures on 2026-08-10 |
| Complete iOS scheme | Passed | 168 unit + 13 UI = green (`TEST SUCCEEDED`); SuspensionGuardTests 8/8 including lifecycle capture/rotation under withLock; xcresult under DerivedData `Test-Vigil-2026.08.10_08-57-40--0400.xcresult` |
| Documentation and plist/privacy lint | Passed | `scripts/check-docs.sh` green for 1.0.0 (24) |
| Signed archive and verification | Pending | Bind to the eventual green merged candidate commit |
| Upload approval | Not requested | Exact app, version, build, commit, and destination approval is required |
| Internal TestFlight acceptance | Pending | Confirm no new app-process `0xdead10cc` after overnight / background use |
| App Review submission approval | Not requested | Separate exact approval after TestFlight acceptance |

## Physical-device additions for this build

1. Leave the app with linked accounts overnight; confirm no SIGKILL / `0xdead10cc` in TestFlight feedback.
2. Background the app during pull-to-refresh and history navigation.
3. Repeat build 23 Settings, widget privacy, pause-automatic-checks, and Grok Build checks.

## Public-release controls

Same as build 23: fictional demo data for listing assets, accurate privacy answers, public privacy/support URLs, and no unsupported-provider claims. No metadata mutation is authorized by this record alone.
