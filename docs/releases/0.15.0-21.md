# Vigil 0.15.0 (21) release record

**Release status: uploaded, processed as `VALID`, and distributed to Internal TestFlight (`IN_BETA_TESTING`). Physical-device acceptance remains pending.**

> Last reviewed: 2026-07-30
>
> Review again: after every completed, failed, or invalidated release gate

This is the canonical evidence record for version `0.15.0`, build `21`.
Build `20` remains documented in [its completed upload record](0.15.0-20.md).

## Identity

| Field | Recorded value |
|---|---|
| App Store Connect Apple ID | `6792373775` |
| Version | `0.15.0` |
| Build | `21` |
| Candidate branch | `release/0.15.0-21-artifact` |
| Artifact candidate | `034658b0f8ec588c5f584ce35310e84ab0c20a47` |
| Integration commit | `18d45968c6624182a4765467f5482ff9ef044747` ([PR #46](https://github.com/ogprotege/vigil/pull/46)) |
| App Store Connect build id | `bdeec8e3-dab9-4829-a5ec-c7492bb73854` |
| Internal TestFlight group | `Internal` |
| Release runbook | [`docs/development/release.md`](../development/release.md) |

## Release scope

Build 21 carries four user-facing repairs merged through PRs #41–#45.

Account removal previously did nothing: the confirmation dialog's action read
its pending account from state that dialog dismissal had already cleared, so
confirming silently removed nothing. Both Accounts-screen dialogs now receive
the account through the dialog's `presenting:` data, demo mode removes
accounts in memory only, and UI tests walk the removal confirmation and the
history-recovery dialog end to end.

Re-linking recovered its missing path: a provider response this build cannot
read (**Provider changed**) previously dead-ended account linking in an
OK-only alert, so removing a drifted account made it impossible to re-add.
That failure now offers **Save anyway**, while rejected credentials still
fail hard.

Degraded freshness became honest on every surface: retained readings age the
data ("Provider changed, data from 1 day ago") instead of claiming a check
time, in the visible line and in the Home card's spoken VoiceOver summary,
which previously replaced the corrected text with the old wording.

Kimi K3 accounts stranded since 2026-07-28 recover: the endpoint's
protobuf-style JSON omits zero-valued numbers, so a zero-usage session
arrived without its `used` field and the strict contract rejected the
response. A sanitized production capture proves the shape, and the
`zeroOmittedNumbers` contract flag accepts an omitted amount as exactly zero
only when the declared limit and the other amount agree. The provider-change
user guide and GitHub issue templates from PR #42 ship alongside.

The fixes landed through [PR #41](https://github.com/ogprotege/vigil/pull/41)
merged as `96c8f6c`, [PR #42](https://github.com/ogprotege/vigil/pull/42)
merged as `be0ab61`, [PR #43](https://github.com/ogprotege/vigil/pull/43)
merged as `9fc8e92`, [PR #44](https://github.com/ogprotege/vigil/pull/44)
merged as `52118c0`, and [PR #45](https://github.com/ogprotege/vigil/pull/45)
merged as `1beff05`.

## Gate status

| Gate | State | Evidence |
|---|---|---|
| Fix pull requests | Merged | PRs #41–#45 merged into `main` at `1beff05`; each ran the full `vigilkit` gate |
| Artifact pull request | Merged | [PR #46](https://github.com/ogprotege/vigil/pull/46) merged as `18d4596`; both `vigilkit` runs on artifact head `034658b` succeeded |
| Exact candidate identity and clean-worktree preflight | Passed | Detached worktree pinned to `034658b0f8ec`; exact HEAD, clean status (untracked included), commit diff check, and tracked-signing-secret scan all passed |
| Build settings from Xcode | Passed | `MARKETING_VERSION=0.15.0`, `CURRENT_PROJECT_VERSION=21`, `VALIDATE_PRODUCT=YES` resolved from `-showBuildSettings` on the pinned worktree; `xcodegen generate` left the tree clean |
| Documentation gate on artifact candidate | Passed | `scripts/check-docs.sh` passed for Vigil 0.15.0 (21) in the pinned worktree |
| VigilKit suite on artifact candidate | Passed | 201 executed, 2 skipped, 0 failures |
| Full iOS scheme on artifact candidate | Passed | 158 passed, 0 failed on iPhone 15 Pro, iOS 17.5 (21F79), simulator `E5A26EA3-3969-450C-B354-AA0EAAFA8F89`; result bundle `Vigil-0.15.0-21-tests.xcresult` in the release directory |
| Property-list, privacy-manifest, and entitlement lint | Passed | `plutil -lint` OK across all six files |
| Merged-main CI | Passed | `vigilkit` run on integration commit `18d4596` completed with success |
| Apple service preflight | Passed | Developer status page all-available at 09:59 and again at 10:23 EDT on 2026-07-30; App Store Connect, App Upload, App Processing, TestFlight, ASC API, and Provisioning Profile Service all `available` |
| Distribution signing preflight | Passed | `Apple Distribution: WILSON WILKERSON WARREN (4KBWH9KYSD)` installed with `Vigil AppStore` and `VigilWidgets AppStore` profiles |
| Fresh signed archive | Passed | `Vigil-0.15.0-21-034658b0f8ec.xcarchive` created ~10:03 EDT from the pinned worktree; `ARCHIVE SUCCEEDED` |
| Archive verifier | Passed | `scripts/verify-ios-archive.sh` passed at 10:03 and again at 10:23 EDT (second pass inside the upload block) |
| App Store Connect collision preflight | Passed | ASC API query at ~10:22 EDT returned zero build-21 records; builds 20/19/18 all `VALID` |
| Immediate pre-upload refresh | Passed | Status re-checked, export options asserted (`upload`, `app-store-connect`, manual signing, team `4KBWH9KYSD`, both profiles), key directory `700` / key `600` outside the repository, second verifier pass |
| Exact upload approval | Approved | Repo owner approved app `6792373775`, version `0.15.0`, build `21`, commit `034658b0f8ec588c5f584ce35310e84ab0c20a47`, Internal TestFlight at 10:21 EDT on 2026-07-30 |
| App Store Connect upload | Passed | `Upload succeeded` and `EXPORT SUCCEEDED` at 10:24:31 EDT; ASC records `uploadedDate 2026-07-30T07:25:10-07:00` |
| Processing and Internal distribution | Passed | Build `bdeec8e3-dab9-4829-a5ec-c7492bb73854` reached `VALID` by 10:26:59 EDT; `buildBetaDetail.internalBuildState=IN_BETA_TESTING`; sole internal all-builds group `Internal` confirmed (`isInternalGroup=true`, `hasAccessToAllBuilds=true`); expiration `2026-10-28` |
| Physical-device acceptance | Pending | Runbook step 11 remains with the repo owner; carried since build 18 |

## Remaining work

The physical-device acceptance walk (runbook step 11) remains with the repo
owner, carried since build 18. Build 21 adds its own on-device checks to that
walk: confirm account removal completes, confirm a drifted provider offers
**Save anyway** on re-add, confirm degraded freshness reads "data from" on
screen and in VoiceOver, and confirm a Kimi K3 account recovers to Live on its
first successful poll of this build.
