# Vigil 0.15.0 (22) release record

**Release status: feature candidate prepared; upload and Internal TestFlight distribution not yet complete.**

> Last reviewed: 2026-08-04
>
> Review again: after every completed, failed, or invalidated release gate

This is the canonical evidence record for version `0.15.0`, build `22`.
Build `21` remains documented in [its completed upload record](0.15.0-21.md).

## Identity

| Field | Recorded value |
|---|---|
| App Store Connect Apple ID | `6792373775` |
| Version | `0.15.0` |
| Build | `22` |
| Candidate branch | `feature/grok-build-provider` (promote to `release/0.15.0-22-artifact` after merge) |
| Artifact candidate | _pending clean merge to `main`_ |
| Integration commit | _pending_ |
| App Store Connect build id | _pending_ |
| Internal TestFlight group | `Internal` |
| Release runbook | [`docs/development/release.md`](../development/release.md) |

## Release scope

Build 22 adds experimental **Grok Build** monitoring so Vigil can track Grok CLI / Grok Build credit usage on the phone.

**Grok Build is not the xAI API prepaid-balance integration.** The existing **xAI API** (`xai`) path still uses a Management Key plus team ID for prepaid balance. Grok Build (`grok`) uses xAI OIDC **device authorization** — the same family of short-code + browser approval flow as desktop `grok login` / `/login` — and polls the Grok Build CLI chat-proxy billing endpoint for monthly credit used/limit (with a percentage-only fallback and optional on-demand metrics).

Setup is a first-class **Connect Grok Build** route. Vigil mints its own renewable session in the iPhone Keychain; it never reads `~/.grok/auth.json`. The integration is labeled Experimental because the billing endpoint is undocumented, even with live-sanitized fixtures.

## Gate status

| Gate | State | Evidence |
|---|---|---|
| Feature implementation | Ready for review | Branch `feature/grok-build-provider`; contract, fixtures, GrokAuth device flow, guided UI, docs |
| VigilKit suite on feature branch | Passed | 206 executed, 2 skipped, 0 failures (2026-08-04) |
| ProviderPresentationTests on feature branch | Passed | 10/10 on iPhone simulator |
| Documentation gate on feature branch | _pending full run after docs freeze_ | `scripts/check-docs.sh` after version/docs alignment |
| Merged-main CI | Pending | Requires PR merge to `main` |
| Exact candidate identity and clean-worktree preflight | Pending | After merge, pin full 40-char commit on a clean worktree |
| Build settings from Xcode | Pending | Expect `MARKETING_VERSION=0.15.0`, `CURRENT_PROJECT_VERSION=22` |
| Full iOS scheme | Pending | Runbook step on pinned artifact |
| Property-list, privacy-manifest, and entitlement lint | Pending | |
| Apple service preflight | Pending | |
| Distribution signing preflight | Pending | |
| Fresh signed archive | Pending | |
| Archive verifier | Pending | |
| App Store Connect collision preflight | Pending | Confirm build 22 does not already exist |
| Exact upload approval | Pending | Owner approval required before upload |
| App Store Connect upload | Pending | |
| Processing and Internal distribution | Pending | |
| Physical-device acceptance | Pending | Include Grok Build connect, monthly credits Live reading, and re-link |

## Device walk additions for this build

On a physical device after Internal TestFlight installs build 22:

1. **Connect Grok Build** — confirm the short code + browser approval path completes and stores a renewable account.
2. Confirm Home shows monthly Grok Build credit utilization after the first successful poll.
3. Confirm the account is labeled Experimental and is distinct from any linked **xAI API** prepaid account.
4. Confirm re-link uses the guided Grok route (not a forced paste of desktop tokens).
5. Carry forward the build 18–21 physical-device checks still open on prior records.

## Related documentation

- [Setup — Connect Grok Build](../user-guide/setup.md#connect-grok-build)
- [Support matrix](../providers/support-matrix.md)
- [Provider details — Grok Build](../providers/provider-details.md)
- [CHANGELOG 0.15.0 (22)](../../CHANGELOG.md)
